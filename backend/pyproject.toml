[project]
name = "kanbanana-backend"
version = "0.1.0"
description = "Kanbanana backend (FastAPI)"
readme = "README.md"
authors = [
    { name = "Muhammad Imran", email = "m.imran.tahir.97@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "httpx>=0.27.0",
]

[tool.uv]
package = false
